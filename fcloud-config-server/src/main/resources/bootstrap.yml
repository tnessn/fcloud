spring:
  cloud:
    config:
      server:
        git: 
          uri: https://github.com/tnessn/fcloud.git 
          search-paths: config-repo
          username: tnessn
          password: xxxxxx
server: 
  port: 2007
encrypt: 
  key: sang
  key-store:
    location: classpath:server.jks
    password: letmein
    alias: mytestkey
    secret: changeme
